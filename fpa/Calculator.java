package com.example.fpa;

public class Calculator {

	public static float calculateValue(float a, float b, float c) {
		// Objective Function : y = 5*a^2 - 10*b + 20*c
		// return ((5 * a * a) - (10 * b) + 20 * c);
		
		// Objective Function : y = a + b + c
		return (a + b + c);
	}
	
}
